# Example credentials — copy to .env and fill in (never commit .env)
#   cp .env.example .env
#   set -a && source .env && set +a
#   ./scripts/collect-aws-state.sh --auto-approve

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
AWS_REGION=eu-central-1

# Optional
# SERVICES=vpc,subnet,sg,ec2,s3
# WORK_DIR=imported
